# Публикация на поддомене (СпринтХост / Таймвеб и любой PHP-хостинг)

Node.js и командная строка **не нужны**. Нужен обычный хостинг с **PHP** (есть на
СпринтХосте и Таймвебе на всех тарифах). Весь сайт — это статические файлы плюс один
PHP-файл `api.php`, который ходит на MOEX (обходит CORS и собирает M5/M15).

## Что загружать

Содержимое папки **`public/`** целиком (не саму папку, а то, что внутри):

```
index.html
api.php
.htaccess          ← скрытый файл, включи в файловом менеджере «показывать скрытые»
css/…
js/…
vendor/…
```

Готовый архив для заливки: **`lun_term_site.zip`** (распакуй в корень поддомена).

## Шаги (одинаковы для СпринтХоста и Таймвеба)

1. **Создай поддомен** в панели хостинга, например `lun.твойдомен.ru`.
   Панель создаст для него папку (корень сайта, docroot) — обычно
   `.../lun.твойдомен.ru/public_html/` или `.../public_html/lun/`.
2. **Загрузи файлы** из `public/` (или распакуй `lun_term_site.zip`) в этот корень
   так, чтобы `index.html` лежал прямо в корне поддомена.
   Не забудь **`.htaccess`** (в файловом менеджере включи показ скрытых файлов).
3. **Выбери PHP ≥ 7.4** для поддомена (в панели: «Версия PHP»). Обычно уже включён
   `curl` — этого достаточно.
4. Открой `https://lun.твойдомен.ru` — появится график.

## Как понять, что данные живые

Смотри строку внизу справа:
- **«Данные: MOEX ISS»** — всё работает, свечи живые, контракт (`SiU6`/`CRU6`)
  определился сам.
- **«ДЕМО — …»** — биржа не отвечает, и **прямо в этой строке написана причина**
  (наведи мышку — покажет полностью). Расшифровка:
  - `…Unexpected token '<' … <?php…` — **PHP не выполняется**, файл отдаётся как текст.
    Значит для поддомена не включён обработчик PHP: в панели выбери версию PHP ≥ 7.4
    (иногда нужно, чтобы в корне лежал `index.php` — но у нас статичный `index.html`,
    поэтому просто убедись, что PHP включён для сайта).
  - `curl: …` или про `allow_url_fopen` — PHP работает, но **закрыт исходящий доступ**.
    Напиши в поддержку СпринтХоста/Таймвеба: «включите исходящие HTTPS-соединения и curl
    для моего сайта».
  - `ISS HTTP 4xx/5xx` — дошли до биржи, но запрос отклонён (неверный тикер или временный
    сбой ISS) — пришли мне текст, поправлю.

  Проверить вручную: открой `https://lun.твойдомен.ru/api.php?fn=front&asset=Si` —
  `{"ticker":"SiU6",…}` значит прокси работает.

## Обновление сайта

Просто перезалей изменившиеся файлы поверх старых (обычно достаточно папки `js/`).

## Проверка вручную (по желанию)

```
https://lun.твойдомен.ru/api.php?fn=front&asset=Si
https://lun.твойдомен.ru/api.php?fn=candles&secid=SiU6&iss=60&from=2026-07-20&till=2026-07-31
```
